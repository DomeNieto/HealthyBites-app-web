export interface Ingredient {
  id: number;
  name: string;
  quantityCalories: number;
  creationDate: string;
}

export interface IngredientFormData {
  name: string;
  quantityCalories: number;
  reationDate: string;
}
